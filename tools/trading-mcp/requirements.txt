# Runtime dependencies for the trading MCP server. Intentionally separate from
# the repository's pyproject.toml: this server is not part of the published
# nemo-switchyard wheel.
mcp>=2.0,<3.0
httpx>=0.27
uvicorn>=0.30
ortools>=9.10
scipy>=1.11
numpy>=1.26

# Only needed for TRADING_MCP_MODE=live; pulls in the web3 signing stack.
# py-clob-client>=0.17
